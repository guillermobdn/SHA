<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sha">

    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraInfoActivity" />
        <activity android:name=".CameraTestActivity" />
        <activity android:name=".CameraResultActivity" />
        <activity android:name=".ScreenInfoActivity" />
        <activity android:name=".ScreenTestActivity" />
        <activity android:name=".ScreenResultActivity" />
        <activity android:name=".BatteryInfoActivity" />
        <activity android:name=".BatteryTestActivity" />
        <activity android:name=".BatteryResultActivity" />
        <activity android:name=".VibratorInfoActivity" />
        <activity android:name=".VibratorTestActivity" />
        <activity android:name=".VibratorResultActivity"></activity>
    </application>

</manifest>